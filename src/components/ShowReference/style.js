import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  option: {
    color: 'black',
  },
  formControl: {
    width: '100%',
  },
  root: {
    color: 'black',
    fontSize: 20,
  },
  icon: {
    color: 'white',
  },
  select: {
    color: 'white',
  },
}));
